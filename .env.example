# Authentication configuration (choose one based on priority)

# Method 1: Use fixed API key (recommended for production, highest priority)
FACTORY_API_KEY=your_factory_api_key_here

# Method 2: Use refresh token for automatic refresh (second priority)
DROID_REFRESH_KEY=your_refresh_token_here
